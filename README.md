# how to startup

from the repo install docksmith executable, 

install bootstrap.sh, 

install Docksmithfile used for the demo

```
chmod +x bootstrap.sh
```

then
```
./bootstrap.sh
```

might have to give password
everything else is done
